# 语义表情包插件用户与管理员手册

## 插件身份与范围

- 插件键：`Akiyo.semantic_sticker`。
- 当前版本：`1.2.4`。
- 作用范围：NekroAgent 实例的全局表情包库，供主 Agent 按场景检索，也负责 Bot 被戳时的直接表情回复。
- Web 管理权限：任何已启用的 NA 登录用户都可以通过 WebUI 上传、修改、重试、重建向量或删除表情包；未登录请求不能访问插件 API。
- 聊天保存权限：普通用户主动保存不要求管理员身份；用户可在同一消息中发送图片并要求保存，也可回复一张图片后要求 Bot 保存。
- 收集边界：主 Agent 只能保存当前消息或当前消息明确引用的图片，不会扫描无关历史图片。是否允许 Agent 自主保存由配置控制。

## 进入管理控制台

先在 NekroAgent WebUI 登录任意已启用账户（普通用户或超级管理员），然后直接打开裸地址：

`/plugins/Akiyo.semantic_sticker/`

该页面为简体中文控制台，包含上传队列、表情包库、筛选、详情编辑、重试、向量重建和删除功能。插件依次从 `?token=`、当前标签页 `sessionStorage["na_console_token"]`、同源 `localStorage["auth-storage"].state.token` 获取访问 Token。

### 同源要求与 NUP 兼容

- 自动复用 NA 登录状态要求插件页面与 NA WebUI 的 `scheme、host、port` 三者完全相同。任何一项不同都属于不同源，浏览器不允许页面读取 NA `auth-storage`。
- 受信任的 NUP 入口仍兼容 `?token=`，且查询 Token 优先级最高。前端取得 Token 后会只从地址栏删除 `token` 参数，其他查询参数和 Hash 保持不变。
- 不同源部署无法读取 NA 本地存储，应使用已授权的 `?token=` 集成，或调整反向代理使插件控制台与 NA WebUI 同源。

### 权限与错误状态

- `GET /` 和白名单静态资源只返回 HTML/CSS/JavaScript 静态外壳，可匿名加载，不包含任何表情包、统计、预览或配置数据。
- 每个 `/api/*` 读取、预览和管理接口仍必须通过 NA 服务端的登录和账号启用检查。普通登录用户与超级管理员都可以使用完整的表情包后台功能；公开静态外壳不等于公开业务数据或写操作。
- HTTP 401 表示未登录或 Token 失效。页面会保留在当前地址，显示“前往 NA 登录”按钮，不自动跳转。
- HTTP 403 不是普通登录用户的正常权限门槛；如果 NA 其他上层策略返回 403，页面不会清除登录状态。

## 部署要求

- 如果通过 nginx 等反向代理暴露管理控制台，`client_max_body_size` 默认只有 1MB，大图或批量上传会被服务器直接拒绝（HTTP 413）。请调大至不低于 `MAX_UPLOAD_BYTES`（默认 10MB），例如 `client_max_body_size 50m;`。
- 控制台前端按单次请求 10MB 提前校验（常量 `DEFAULT_MAX_REQUEST_BYTES`，位于 `web/app.js` 顶部）；如服务器限制不同，请同步修改该常量，使前端提示与实际限制一致。
- 单次上传最多 `MAX_UPLOAD_FILES`（20）个文件，单文件大小受 `MAX_UPLOAD_BYTES` 限制。

## 日常管理流程

### 上传表情包

1. 在“上传并自动分析”区域拖入图片，或点击“选择文件”。
2. 支持 PNG、JPEG、GIF、WebP，可一次选择多个文件。
3. 上传完成后文件进入后台分析队列，页面会显示每个文件的处理状态。

### 自动模型分类

视觉模型会自动生成描述、主分类、情绪标签、场景标签、OCR 文本、适用场景、不适用场景和安全状态。只有达到可用状态且安全的记录才进入正常检索范围。聊天保存与 Web 上传使用完全相同的分析、去重和索引流水线。

### 人工修改标签

在表情包卡片中打开“表情包详情”，可修改描述、主分类、情绪标签、场景标签、OCR 文本及适用或不适用场景。保存前必须填写修订原因，便于管理员审计。

### 重试分析

处理失败或标签不合适时，打开详情并点击“重新模型分析”。该操作会再次调用视觉模型。

### 重建向量

- 单张记录：点击“仅重建向量”，不会重新调用视觉模型。
- 全部记录：点击“全量重建向量”，只处理当前可用且安全的表情包。
- 修改描述或标签后，如需让检索立即使用新语义，应执行重建向量。

### 删除表情包

可在详情中删除单张，也可勾选多张后批量删除。删除会移除原图、缩略图和向量；审计记录仍保留。

## 聊天中保存表情包

### 同一消息保存

用户发送图片并附带“保存这个表情包”“把这张存起来”等明确要求。主 Agent 会从当前消息图片中选择目标，并调用保存工具。

### 回复图片保存

用户回复或引用一条带图片的消息，再发送“保存这个表情包”。主 Agent 只能从被引用消息的图片中选择，不会模糊搜索其他历史图片。

### Agent 自主保存

当 `AUTO_COLLECT_ENABLED` 开启时，主 Agent 可像 emotion 插件一样，在看清图片且认为它是有用表情包时自主保存。关闭后不会自行保存，但用户明确提出的保存请求仍然有效。

### 严格模式

当 `STRICT_EMOTION_COLLECT` 开启时，主 Agent 必须通过视觉确认图片是表情包或 reaction GIF。截图、照片、普通图片或无法确认内容的图片会被拒绝。关闭严格模式后仍只能选择当前消息或明确引用的图片，并继续经过既有格式校验和安全分析。

重复图片按 SHA-256 去重；已保存过的图片不会重复创建资产。

## Agent 回复模式

主 Agent 根据当前对话场景自由选择以下三种模式：

- `image_only`：只发送匹配的表情包，不附带文字。
- `text_then_image`：先发送文字，再发送匹配的表情包。
- `auto`：由主 Agent 根据语境决定是否发送文字及表情包。

这些模式共用同一套语义检索、重复抑制、物理频道冷却和使用历史。

## Bot 被戳时的行为

当 Bot 被戳时，插件直接拦截 OneBot 戳一戳事件，按“惊讶、疑惑、调皮或轻微不满”的场景检索并发送一张表情包。该路径不创建聊天消息、不请求主 Agent，也不调用 LLM。

没有候选、仍处于 CD、图片读取失败或发送失败时保持静默。事件仍会被拦截，**不回退 LLM**。

## 关键配置

### `ANALYSIS_MODEL_GROUP`

用于自动识别内容、分类和打标签的聊天模型组。NekroAgent 配置页中显示为聊天模型组下拉框。

### `EMBEDDING_MODEL_GROUP`

用于语义检索和向量索引的嵌入模型组。NekroAgent 配置页中显示为嵌入模型组下拉框；`VECTOR_DIMENSION` 必须与所选模型一致。

### `AUTO_COLLECT_ENABLED`

- `true`：允许主 Agent 自主保存已确认的表情包。
- `false`：禁止自主保存，但普通用户明确要求保存仍然有效。

### `STRICT_EMOTION_COLLECT`

- `true`：拒绝截图、照片、普通图片和视觉未确认的内容。
- `false`：不要求主 Agent 提交严格视觉确认，但仍执行图片格式、安全和大小校验。

### `PHYSICAL_CHANNEL_COOLDOWN_SECONDS`

同一物理频道连续发送表情包的冷却时间，单位为秒。Agent 工具与 Bot 被戳后的直接表情回复共享该值。

- 0：关闭冷却，仍保留同一事件防重复发送。
- 20：默认值，适合一般群聊。
- 60：设置为 60 秒，适合希望明显降低表情包频率的频道。

不允许填写负数。修改后应仅重建目标 Agent。

### `SEMANTIC_SCORE_THRESHOLD`

语义候选的最低相似度阈值。数值越高匹配越严格；过高可能无候选，过低可能出现不贴合场景的表情包。

### `RECENT_SELECTION_WINDOW`

近期选择去重窗口，用于降低短时间内重复发送同一表情包的概率。

## 配置文件位置与修改

实际运行配置文件（位于 NekroAgent 实例目录下）：

`plugin_data/Akiyo.semantic_sticker/config.yaml`

优先在 NekroAgent 插件配置后台修改并保存。所有配置项均提供中文名称和中文说明，两个模型组使用下拉框。若必须直接修改 YAML：

1. 先备份原配置文件并记录 SHA-256。
2. 只修改目标字段，不改其他插件、频道或实例。
3. 布尔值保持 YAML 布尔类型，数值字段保持数值类型。
4. 修改后检查 YAML 语法和目标字段，再仅重建目标 Agent。

以下文件仅用于插件启用列表（位于 NekroAgent 实例目录下）：

- `configs/nekro-agent.yaml`

发布或回滚插件启用状态时，这份启用配置必须同步修改和校验。管理员不应为调整 CD 手工修改该文件，也不要在其中设置模型组、自动保存、严格模式、语义阈值或近期去重窗口。

不要把容器内临时文件当作配置源，也不要重启 PostgreSQL、Qdrant、NapCat 或其他 NekroAgent 实例。

## 仅重建目标 Agent

在部署实例的服务器上执行：

```bash
cd <NekroAgent 实例目录>
docker compose up -d --no-deps --force-recreate --pull never nekro_agent
```

该命令只重建目标 `nekro_agent` 服务，不拉取新镜像，也不重建依赖服务。完成后检查容器健康、重启计数、OOM 状态和插件日志。

## 回滚

1. 恢复发布前备份的插件目录并核对哈希。
2. 如需回滚配置，只恢复实例目录下的 `plugin_data/Akiyo.semantic_sticker/config.yaml`。
3. 如需回滚插件启用状态，同步恢复两份启用列表并核对哈希。
4. 保留表情包数据目录，不要删除管理员上传或用户保存的数据。
5. 仅重建目标 Agent。
6. 验证插件版本、配置、启用列表和容器健康；不要重启 PostgreSQL、Qdrant、NapCat 或其他实例。
