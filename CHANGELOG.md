# 更新日志

## 1.2.4（2026-08-19）

- 修复：表情包 WebUI 的 `/api/*` 现在只要求 NA 账户已登录且处于启用状态，不再要求超级管理员权限。普通登录用户可以使用完整的上传、编辑、删除、重分析和向量重建功能。
- 安全：未登录或 Token 失效的请求仍由 NA 认证依赖拒绝，前端继续保留同源 `auth-storage` 和受信任 `?token=` 兼容入口。

## 1.2.3（2026-08-13）

- 修复：将真实插件包直接发布在仓库根目录；NekroAgent 插件市场克隆整个仓库后，
  可直接加载根 `__init__.py`、同级模块与 `web/` 静态资源。
- 结构：移除 `source/nekro_plugin_semantic_sticker/` 嵌套层，不保留转发包装或重复源码。
- 文档：详细用户与管理员手册移至根目录 `USER_GUIDE.md`。
- 测试：新增市场目录形态的独立进程导入回归测试，防止发布结构再次退化。

## 1.2.2（2026-08-13）

- 修复：已在同源 NekroAgent WebUI 登录的超级管理员，现在可直接输入 `/plugins/Akiyo.semantic_sticker/` 裸地址，插件会复用 `auth-storage` 中的 NA 登录 Token。
- 安全：仅静态外壳允许匿名加载，所有 `/api/*` 数据、预览和管理接口继续要求 NA 超级管理员。
- 兼容：保留 NUP `?token=` 入口，前端启动后从地址栏清理 Token；HTTP 401 显示登录提示，HTTP 403 单独显示超级管理员权限不足。
- 修复：上传后状态轮询遇到 HTTP 401/403 时立即停止，避免登录失效或权限不足后继续请求受保护接口。

## 1.2.1（2026-08-05）

- 修复：管理控制台大文件或批量上传时被反向代理默认限制（nginx `client_max_body_size` 1MB）直接拒绝（HTTP 413），且界面只显示笼统失败提示的问题；前端现在会在提交前按单次请求总量提前校验并给出明确中文提示。
- 改进：上传区增加单次请求总量说明；HTTP 413 错误提示改为可操作的部署指引。
- 文档：README 新增“部署要求”小节，说明 `client_max_body_size` 与前端校验常量 `DEFAULT_MAX_REQUEST_BYTES` 的配置方法。
- 说明：本版本为公开仓库首个发布版本，已移除实例专属标识，可作为通用 NekroAgent 插件使用。

## 1.2.0（2026-08-04）

- 首个正式版本：网页管理控制台、语义检索、Agent 自主收集、用户主动保存、戳一戳直接表情回复。