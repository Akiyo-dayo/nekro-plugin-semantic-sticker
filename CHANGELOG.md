# 更新日志

## 1.2.1（2026-08-05）

- 修复：管理控制台大文件或批量上传时被反向代理默认限制（nginx `client_max_body_size` 1MB）直接拒绝（HTTP 413），且界面只显示笼统失败提示的问题；前端现在会在提交前按单次请求总量提前校验并给出明确中文提示。
- 改进：上传区增加单次请求总量说明；HTTP 413 错误提示改为可操作的部署指引。
- 文档：README 新增“部署要求”小节，说明 `client_max_body_size` 与前端校验常量 `DEFAULT_MAX_REQUEST_BYTES` 的配置方法。
- 说明：本版本为公开仓库首个发布版本，已移除实例专属标识，可作为通用 NekroAgent 插件使用。

## 1.2.0（2026-08-04）

- 首个正式版本：网页管理控制台、语义检索、Agent 自主收集、用户主动保存、戳一戳直接表情回复。